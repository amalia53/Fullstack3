{
  "dependencies": {
    "mongoose": "^6.6.1"
  },
  "devDependencies": {
    "eslint": "^8.24.0"
  },
  "scripts": {
    "start": "cd backend && npm start && cd ../frontend && npm start",
    "deploy": "cd backend && rm -rf build && cd ../frontend && npm run build && cp -r build ../backend && cd ../ && git add . && git commit -m uibuild && git push && cd backend && npm run deploy"
  }
}
