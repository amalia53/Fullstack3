{
  "dependencies": {
    "mongoose": "^6.6.1"
  },
  "devDependencies": {
    "eslint": "^8.24.0"
  },
  "scripts": {
    "build:ui": "cd backend && rm -rf build && npm install && cd ../frontend && npm install && npm run build && cp -r build ../backend && cd ../",
    "deploy:full": "npm run build:ui"
  }
}
